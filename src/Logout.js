import Button from "@mui/material/Button";
import Box from "@mui/material/Box";

const Logout = () => {
  return(
    <Box m={0} pt={0}>
      <Button href="https://backend0322.herokuapp.com//logout" variant="outlined">Logout</Button>
    </Box>
  )
};

export default Logout;